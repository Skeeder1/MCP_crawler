# ============================================================================
# MCP Servers Auto-Collector - Configuration d'Environnement
# ============================================================================
# INSTRUCTIONS:
# 1. Copier ce fichier vers .env
# 2. Remplacer les valeurs par vos vrais tokens/clés
# 3. NE JAMAIS commiter .env (déjà dans .gitignore)
# ============================================================================

# === REQUIS ===================================================================

# GitHub Personal Access Token
# Obtenir: https://github.com/settings/tokens
# Permissions: public_repo (lecture repos publics)
# Rate limit: 5000 req/h avec token vs 60 sans
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx


# === OPTIONNEL ================================================================

# npm Token (améliore les résultats mais pas obligatoire)
# Obtenir: https://www.npmjs.com/settings/YOUR_USERNAME/tokens
# npm API publique fonctionne sans token
NPM_TOKEN=npm_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# MCP Registry API Key (si l'API officielle le requiert)
# Vérifier: https://registry.modelcontextprotocol.io/docs
MCP_REGISTRY_API_KEY=


# === CONFIGURATION ============================================================

# Niveau de logging (DEBUG | INFO | WARNING | ERROR | CRITICAL)
LOG_LEVEL=INFO

# Activer le cache HTTP (true/false)
# Recommandé: true pour éviter de refetch les mêmes URLs
ENABLE_CACHE=true

# Durée de vie du cache en heures
CACHE_TTL_HOURS=24

# Nombre maximum de requêtes HTTP parallèles
# Recommandé: 10 pour éviter de surcharger les APIs
MAX_CONCURRENT_REQUESTS=10

# Seuil de rate limit GitHub avant pause (sur 5000)
# Si remaining < ce seuil, pause de 60s
GITHUB_RATE_LIMIT_THRESHOLD=100

# Délai entre requêtes (en secondes)
# Recommandé: 1 pour GitHub, 0.5 pour npm
REQUEST_DELAY_SECONDS=1.0


# === CHEMINS (Optionnel - utilise defaults si non défini) ====================

# Chemin vers le dossier de cache
# CACHE_DIR=./data/cache

# Chemin vers le dossier de logs
# LOGS_DIR=./data/logs

# Chemin vers le dossier de backups
# BACKUPS_DIR=./data/backups


# === FILTRES (Optionnel) ======================================================

# Serveurs minimum GitHub stars pour être inclus
# MIN_GITHUB_STARS=0

# Exclure serveurs sans commit récent (en jours)
# MAX_DAYS_SINCE_COMMIT=365

# Exclure serveurs dépréciés/archivés
# EXCLUDE_ARCHIVED=true


# === NOTIFICATIONS (Optionnel - Phase avancée) ================================

# Slack Webhook pour notifications (erreurs critiques)
# SLACK_WEBHOOK_URL=

# Email pour alertes (avec SendGrid, Mailgun, etc.)
# ALERT_EMAIL=

# SendGrid API Key
# SENDGRID_API_KEY=


# === DEBUG ====================================================================

# Mode debug (affiche plus d'infos, désactive rate limiting)
# ATTENTION: Utiliser uniquement en dev, pas en prod!
# DEBUG_MODE=false

# Limiter nombre de serveurs traités (pour tests rapides)
# 0 = pas de limite
# DEBUG_MAX_SERVERS=5
